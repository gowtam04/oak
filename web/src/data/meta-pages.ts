/**
 * Retired. Public competitive usage is `/usage` (live Champions T15, ADR-5).
 * `/meta` permanently redirects there. Smogon `meta_snapshot` / `meta_usage`
 * and `meta-repo` are gone (CF-INT-BR-7).
 */

export {};
